package com.rc.wellnestmodule.interfaces

import android.view.View

interface IWellnestGraph {

    //set this method for directly showing graph recycler view inside your app.
    fun setGraphView(view: View)
}